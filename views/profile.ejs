<!doctype html>
<html>
<head>
	<title>Profil</title>
    <% include layout%>
</head>
<body>
	<div class="wrapper">	
		<% if (user.roleU == "ETUDIANT"){ %>
			<% include Menus/MenuEtudiant %>
		<% } else if (user.roleU == "ENSEIGNANT"){ %>
			<% include Menus/MenuEnseignant %>
		<% } else { %>
			<% include Menu %>
		<% } %>
		<!-- Page Content  -->
		<div id="content">
			<div class="container">
				<h1> Profil</h1>
				<div class="row">
					<div class="col-sm-8">
						<div class="well">
							<h3><span class="fa fa-user"></span> </h3>
							<p>
								<strong>Nom</strong>: <%= user.nomU %><br>
								<br>
								<strong>Prénom</strong>: <%= user.prenomU %><br>
								<% if (user.roleU == "ETUDIANT"){ %>
									<strong>Promo</strong>: <%= user.nomP %>
								<% } %>
							</p>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</body>
</html>
